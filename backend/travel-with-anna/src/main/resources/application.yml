spring:
  profiles:
    active: oauth2-prod
  servlet:
    multipart:
      max-file-size: 50MB
springdoc:
  default-produces-media-type: application/json
server:
  port: ${PORT:8080}
  servlet:
    context-path: /twa/v1/
